<!DOCTYPE html>
<html lang="pt">
  <head>
    <title>Instagram</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="UTF-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'authentication', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "authentication", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag "turbo", type: "module" %>
  </head>

  <body data-flash-message="<%= JSON.dump(flash.to_a) %>">
    <main>
        <section class="hero">
            <div class="logo">
                <%= image_tag "instagram.png"%>
            </div>
            <%= yield %>
        </section>
    </main>
  </body>
</html>
